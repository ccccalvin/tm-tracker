# Dependencies
node_modules/

# Build output
dist/

# Source PDFs — these live in Firebase Storage, NOT in the repo (866 MB).
# The catalog (src/data/catalog.json) is generated from these filenames and IS
# committed; the PDFs themselves are uploaded via scripts/upload-pdfs.mjs.
yr12-advn-trials/
yr12-ext1-trials/
**/*.pdf

# Local env / secrets
.env
.env.local
serviceAccountKey.json

# Firebase emulator state
.emulator-data/
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log

# Design-sync tooling (local working dirs: caches, node_modules, built bundles)
.design-sync/
.ds-sync/
ds-bundle/

# Editor / OS
.DS_Store
*.tsbuildinfo

# Stale compiled config (tsc emits these from vite.config.ts; Vite must use the .ts)
vite.config.js
vite.config.d.ts
